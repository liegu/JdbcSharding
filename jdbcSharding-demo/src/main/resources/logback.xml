<?xml version="1.0" encoding="UTF-8"?>
<!-- 
 Logger Level
    TRACE < DEBUG < INFO < WARN < ERROR
 -->
<configuration scan="true" scanPeriod="60 seconds" debug="TRUE">

    
	<!-- logback会自动创建文件夹，这样设置了就可以输出日志文件了 -->
	<substitutionProperty name="logbase" value="${pom.log.dir}" />
	
	<!-- 这个是要配置输出文件的-->
	<jmxConfigurator /> 
	 
	<appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
		<!-- 
		<cyclicBufferTracker class="ch.qos.logback.core.spi.CyclicBufferTrackerImpl">
	      <maxNumberOfBuffers>4096</maxNumberOfBuffers>
	    </cyclicBufferTracker>
		 -->
		<layout class="ch.qos.logback.classic.PatternLayout">
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36}.%M - %msg%n</pattern>
		</layout>
	</appender>
	
	<!-- 文件输出日志 -->
	<appender name="LogFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<Encoding>UTF-8</Encoding>
		
		<!-- 每天生成一个日志文件，保存31天的日志文件。 -->
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
		    <!-- log.dir 在maven profile里配置-->
		    			<FileNamePattern>${logbase}_%d{yyyy-MM-dd}.log</FileNamePattern>
            <!-- 日志最大的历史 60天 -->
			<!-- 
			<MaxHistory>31</MaxHistory>
			 -->
		</rollingPolicy>

		<!-- 
		<triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
			<MaxFileSize>50MB</MaxFileSize>
		</triggeringPolicy>
		 -->	
		<!-- 
		<layout class="ch.qos.logback.classic.html.HTMLLayout">
		 -->
		<layout class="ch.qos.logback.classic.PatternLayout">
			<pattern>%date{yyyy-MM-dd hh:mm} %-5level %logger{36}.%M - %msg%n</pattern>
		</layout>
	</appender>
	
	<!-- 错误记录 -->
	<appender name="LogErrorFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
	      <filter class="ch.qos.logback.classic.filter.LevelFilter">
	         <level>DEBUG</level>
	         <OnMismatch>DENY</OnMismatch>
	         <OnMatch>ACCEPT</OnMatch>
	     </filter>
	     
	     <File>${logbase}_error.log</File>
	     <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
	         <FileNamePattern>${logbase}-Error.log.%i.zip</FileNamePattern>
	         <MinIndex>1</MinIndex>
	         <MaxIndex>3</MaxIndex>
	     </rollingPolicy>
	     <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
	         <MaxFileSize>100MB</MaxFileSize>
	     </triggeringPolicy>
	     <layout class="ch.qos.logback.classic.PatternLayout">
	         <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n</Pattern>
	     </layout>
	</appender>
	
	<!-- 错误记录 -->
	<appender name="accessLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
	      <filter class="ch.qos.logback.classic.filter.LevelFilter">
	         <level>${pom.log.level}</level>
	         <OnMismatch>DENY</OnMismatch>
	         <OnMatch>ACCEPT</OnMatch>
	     </filter>
	     
	     <File>${logbase}_access.log</File>
	     <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
	         <FileNamePattern>${logbase}-access.log.%i.zip</FileNamePattern>
	         <MinIndex>1</MinIndex>
	         <MaxIndex>3</MaxIndex>
	     </rollingPolicy>
	     <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
	         <MaxFileSize>100MB</MaxFileSize>
	     </triggeringPolicy>
	     <layout class="ch.qos.logback.classic.PatternLayout">
	         <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36}.%M - %msg%n</Pattern>
	     </layout>
	</appender>
	
	<!-- Output by Email -->
	<!-- 
	<appender name="Email" class="ch.qos.logback.classic.net.SMTPAppender"> 
		<SMTPHost>${mail.smtphost}</SMTPHost>
		<To>${mail.default.to}</To> 
		<From>${mail.default.from}</From>
		<Subject>DataService-Error-LogMessage: %logger{20} - %m</Subject>
		<username>${mail.username}</username>
		<password>${mail.password}</password>
		<asynchronousSending>true</asynchronousSending>
		
		<layout class="ch.qos.logback.classic.PatternLayout">
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36}.%M - %msg%n</pattern>
		</layout>
	</appender>
	 -->
	
	<!-- Output to Database -->
	<!-- 
	<appender name="DB" class="ch.qos.logback.classic.db.DBAppender">
		<connectionSource class="ch.qos.logback.core.db.DriverManagerConnectionSource">
			<driverClass>com.mysql.jdbc.Driver</driverClass> 
			<url>jdbc:mysql://localhost:3306/test</url>
			<user>root</user>
			<password>trend_dev</password> 
		</connectionSource>
	</appender>
	 -->
	
	<!-- 需要记录日志的包 -->
	<logger name="com.petesky">
		<level value="INFO" />
	</logger>
	
	<logger name="org.springframework">
		<level value="ERROR" />
	</logger>

    <logger name="org.apache">
        <level value="ERROR" />
    </logger>

	<!-- org.apache.http.wire.wire
	 通过设置additivity="false"可以让日志不向上一级别的logger输出！
	 -->
	<root>
		<level value="${pom.log.level}" />
		<appender-ref ref="Console" />
		<appender-ref ref="LogFile" />
		<appender-ref ref="LogErrorFile" />
		<appender-ref ref="accessLog" />
	</root>
</configuration>